# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
В данном разделе описаны интерфейсы данных, которые были разработаны при разборе проекта, а также указано их прямое назначение в приложении.

#### Интерфейс товара (`IProduct`)
* **Назначение:** Описывает структуру данных отдельного товара (карточки), загружаемого с внешнего сервера и используемого для отображения на витрине, в модальном окне подробного описания или внутри списка корзины.
```typescript
interface IProduct {
  id: string;                 // Уникальный идентификатор товара
  description: string;        // Подробное текстовое описание товара для модального окна
  image: string;              // URL-ссылка на изображение товара
  title: string;              // Название (наименование) товара
  category: string;           // Категория товара (например, "софт-скилл", "кнопка")
  price: number | null;       // Цена товара (может быть null, если товар бесценный)
}
```

#### Тип способа оплаты (`TPayment`)
* **Назначение:** Ограничивает возможные варианты оплаты заказа.
```typescript
type TPayment = 'card' | 'cash'; // 'card' — онлайн, 'cash' — при получении
```

#### Интерфейс покупателя (`IBuyer`)
* **Назначение:** Описывает персональные, контактные и платежные данные покупателя, которые заполняются пользователем на шагах оформления заказа через формы приложения.
```typescript
interface IBuyer {
  payment: TPayment | null;   // Вид оплаты (изначально null, выбирается пользователем)
  email: string;              // Электронная почта покупателя
  phone: string;              // Номер телефона для связи
  address: string;            // Адрес доставки товара
}
```

#### Тип ошибок валидации (`TBuyerErrors`)
* **Назначение:** Структура объекта с ошибками валидации формы покупателя. Ключи — поля `IBuyer`, значения — тексты ошибок.
```typescript
type TBuyerErrors = Partial<Record<keyof IBuyer, string>>;
```

#### Интерфейс полного заказа (`IOrder`)
* **Назначение:** Расширяет интерфейс данных покупателя (`IBuyer`), добавляя информацию о составе корзины и итоговой стоимости для последующей отправки на сервер.
```typescript
interface IOrder extends IBuyer {
  total: number;              // Итоговая сумма заказа
  items: string[];            // Массив уникальных ID покупаемых товаров
}
```

#### Интерфейс ответа сервера при заказе (`IOrderResult`)
* **Назначение:** Описывает структуру объекта, который приходит от сервера в ответ на успешное оформление покупки.
```typescript
interface IOrderResult {
  id: string;                 // Уникальный идентификатор созданного заказа
  total: number;              // Итоговая списанная сумма покупки
}
```

#### Интерфейс ответа сервера со списком товаров (`IApiProductsResponse`)
* **Назначение:** Описывает структуру объекта, возвращаемого сервером при GET-запросе каталога товаров.
```typescript
interface IApiProductsResponse {
  total: number;              // Общее количество доступных товаров на сервере
  items: IProduct[];          // Массив объектов самих товаров
}
```

#### Базовый интерфейс API (`IApi`) и тип методов (`ApiPostMethods`)
* **Назначение:** Описывает методы, реализуемые базовым классом `Api`.
```typescript
type ApiPostMethods = 'POST' | 'PUT' | 'DELETE';

interface IApi {
  get<T extends object>(uri: string): Promise<T>;
  post<T extends object>(uri: string, data: object, method?: ApiPostMethods): Promise<T>;
}
```

#### Интерфейс брокера событий (`IEvents`)
* **Назначение:** Описывает контракт брокера событий, используемого для связи слоёв Model, View и Presenter. Объявлен в шаблонном файле `src/components/base/Events.ts` вместе с классом-реализацией `EventEmitter` и вспомогательными типами `EventName`, `Subscriber`, `EmitterEvent`.
* **Примечание:** В разделе «Базовый код» перечислены только те методы `EventEmitter`, которые активно используются в приложении. Полный контракт интерфейса `IEvents` приведён ниже.

```typescript
interface IEvents {
  on<T extends object>(event: EventName, callback: (data: T) => void): void;
  off(event: EventName, callback: Subscriber): void;
  emit<T extends object>(event: string, data?: T): void;
  onAll(callback: (event: EmitterEvent) => void): void;
  offAll(): void;
  trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void;
}
```

#### Интерфейсы слоя Представления
* **Назначение:** Описывают данные, передаваемые в компоненты Представления через метод `render`, а также структуру событий и внутренних состояний.

```typescript
// Данные шапки сайта
interface IHeaderData {
  counter: number;
}

// Данные галереи каталога
interface IGalleryData {
  catalog: HTMLElement[];
}

// Данные модального окна
interface IModalData {
  content: HTMLElement;
}

// Данные корзины
interface IBasketView {
  items: HTMLElement[];
  total: number;
  disabled: boolean;
}

// Данные окна успешного заказа
interface ISuccess {
  total: number;
}

// Обработчики действий карточки товара
interface ICardActions {
  onClick: (e: MouseEvent) => void;
}

// Данные карточки превью
interface ICardPreviewData extends IProduct {
  buttonText: string;
  buttonDisabled: boolean;
}

// Данные карточки корзины
interface ICardBasketData extends IProduct {
  index: number;
}

// Состояние формы
interface IFormState {
  valid: boolean;
  errors: string[];
}

// Поля первой формы заказа
interface IOrderForm {
  payment: TPayment | null;
  address: string;
}

// Поля формы контактов
interface IContactsForm {
  email: string;
  phone: string;
}
```

### Модели данных
В данном разделе описаны все классы, которыми реализована работа с данными в приложении. Они отвечают за хранение информации и бизнес-логику работы с ней. При любом изменении своего внутреннего состояния модели реактивно генерируют события через брокер событий (паттерн Наблюдатель), уведомляя Презентер о необходимости перерисовки интерфейса. Модели не отдают данные напрямую в события — событие сообщает только о факте изменения, а данные Презентер получает через методы моделей.

#### Класс Каталог товаров (`CatalogModel`)
* **Назначение и зона ответственности:** Отвечает за хранение полного массива товаров, доступных для покупки в приложении, а также за хранение товара, выбранного пользователем для подробного отображения в модальном окне.
* **Конструктор класса:** `constructor(events: IEvents)` — принимает экземпляр брокера событий для уведомления системы об изменениях.
* **Поля класса:**
  * `private items: IProduct[]` — массив всех товаров, доступных в приложении.
  * `private preview: IProduct | null` — товар, выбранный для подробного отображения (или `null`).
  * `private events: IEvents` — брокер событий для отправки уведомлений.
* **Методы класса:**
  * `setItems(items: IProduct[]): void` — сохраняет массив товаров внутри модели и генерирует событие `items:changed` без данных.
  * `getItems(): IProduct[]` — возвращает текущий сохраненный массив всех товаров из модели.
  * `getItemById(id: string): IProduct | undefined` — возвращает один конкретный товар по его id.
  * `setPreview(item: IProduct | null): void` — сохраняет выбранный товар для подробного отображения и генерирует событие `preview:changed` без данных. Презентер получает товар методом `getPreview()`.
  * `getPreview(): IProduct | null` — возвращает товар, выбранный в данный момент для подробного отображения.

#### Класс Корзина (`BasketModel`)
* **Назначение и зона ответственности:** Отвечает за хранение массива товаров, которые пользователь выбрал для покупки, а также реализует методы для управления этим списком и подсчета итоговых метрик.
* **Конструктор класса:** `constructor(events: IEvents)` — принимает экземпляр брокера событий для уведомления системы об изменениях.
* **Поля класса:**
  * `private basketItems: IProduct[]` — массив товаров, выбранных покупателем для покупки.
  * `private events: IEvents` — брокер событий для отправки уведомлений.
* **Методы класса:**
  * `getBasketItems(): IProduct[]` — возвращает массив всех товаров, которые находятся в корзине в текущий момент.
  * `addBasketItem(item: IProduct): void` — добавляет товар в массив корзины и генерирует событие `basket:changed`.
  * `removeBasketItem(id: string): void` — удаляет товар из массива корзины по его id и генерирует событие `basket:changed`.
  * `clearBasket(): void` — производит полную очистку корзины и генерирует событие `basket:changed`.
  * `getTotalPrice(): number` — рассчитывает и возвращает суммарную числовую стоимость всех товаров в корзине.
  * `getBasketCount(): number` — возвращает текущее количество товаров в корзине.
  * `isInBasket(id: string): boolean` — выполняет проверку наличия товара в корзине по его id.

#### Класс Покупатель (`BuyerModel`)
* **Назначение и зона ответственности:** Отвечает за хранение данных покупателя, которые тот должен указать при оформлении заказа (вид оплаты, адрес, телефон, email).
* **Конструктор класса:** `constructor(events: IEvents)` — принимает экземпляр брокера событий для уведомления системы об изменениях.
* **Поля класса:**
  * `private payment: TPayment | null` — приватное поле для хранения вида оплаты.
  * `private email: string` — приватное поле для хранения email покупателя.
  * `private phone: string` — приватное поле для хранения телефона покупателя.
  * `private address: string` — приватное поле для хранения адреса доставки.
  * `private events: IEvents` — брокер событий для отправки уведомлений.
* **Методы класса:**
  * `setBuyerField(field: keyof IBuyer, value: string): void` — сохраняет данные конкретного поля в модели и генерирует событие `buyer:changed`.
  * `getBuyerData(): IBuyer` — возвращает все накопленные данные покупателя в виде единого объекта.
  * `clearBuyerData(): void` — производит полную очистку данных покупателя, сбрасывая их в начальное состояние, и генерирует событие `buyer:changed`.
  * `validateBuyerData(): TBuyerErrors` — выполняет валидацию данных. Метод динамически формирует и возвращает объект типа `TBuyerErrors`. Проверяется заполненность полей: выбран ли способ оплаты, введены ли email, телефон и адрес. Если ошибок нет, возвращается пустой объект `{}`.

### Слой коммуникации
В данном разделе описан класс, выступающий представителем коммуникационного слоя приложения «Web-Larek». Он отвечает за отправку запросов на сервер и получение ответов, изолируя сетевую логику от остального кода приложения.

#### Класс `ApiWebLarek`
* **Назначение и зона ответственности:** 
  Класс представляет собой представитель коммуникационного слоя приложения «Web-Larek». Использует паттерн «Композиция» для работы с сетью, не привязываясь напрямую к конкретной реализации сетевого класса. Отвечает за получение актуального каталога товаров с сервера, а также за отправку сформированного заказа покупателя.

* **Конструктор класса:**
  `constructor(api: IApi)` — принимает объект, соответствующий интерфейсу `IApi`, содержащий методы `get` и `post` для отправки и получения данных.

* **Поля класса:**
  * `private api: IApi` — приватное поле для хранения переданного сетевого объекта коммуникации.

* **Методы класса:**
  * `getProductList(): Promise<IApiProductsResponse>` — выполняет GET-запрос на эндпоинт `/product` через объект `api`. Возвращает промис с объектом `IApiProductsResponse`, полученным от сервера, внутри которого находится массив товаров `items` и их общее количество `total`.
  * `createOrder(order: IOrder): Promise<IOrderResult>` — принимает объект оформленного заказа `order` и отправляет его POST-запросом на эндпоинт `/order` через объект `api`. Возвращает промис с объектом `IOrderResult`, подтверждающим покупку на определенную сумму.

### Слой представления (View)
Все компоненты слоя представления наследуются от базового абстрактного класса `Component<T>` и отвечают за отображение интерфейса на странице, поиск элементов в DOM-дереве, установку текстовых и визуальных значений и первичную обработку действий пользователя. Взаимодействие со связующим слоем (Презентером) строится на генерации событий или через коллбэк, переданный в конструктор.

Поля классов представления хранят исключительно ссылки на HTML-элементы. Компоненты не хранят внутри себя состояние данных и не принимают самостоятельных решений об изменении разметки. Каждый класс размещён в отдельном файле и отвечает только за свой блок разметки.

#### 1. Группа компонентов Карточки товара
Базовый класс объединяет общую логику отображения, а дочерние классы адаптируют её под конкретные шаблоны (темплейты) вёрстки. Общий функционал вынесен в абстрактного родителя, промежуточный родитель добавляет функционал для карточек с картинкой и категорией.

* **Базовый класс Карточка (`Card<T>`)** — абстрактный.
  * **Назначение:** Описывает общие элементы карточки товара, используемые во всех шаблонах приложения — название и цену.
  * **Конструктор:** `constructor(container: HTMLElement)` — принимает контейнер разметки. Находит элементы в конструкторе и сохраняет их в поля.
  * **Поля класса:**
    * `protected titleElement: HTMLElement` — элемент названия товара.
    * `protected priceElement: HTMLElement` — элемент отображения цены.
  * **Методы (Сеттеры):**
    * `set title(value: string)` — устанавливает название товара.
    * `set price(value: number | null)` — выводит цену в формате `X синапсов` или строку `Бесценно`, если цена равна `null`.

* **Промежуточный класс Карточка с изображением (`CardWithImage<T>`)** — абстрактный, наследуется от `Card`.
  * **Назначение:** Описывает общие элементы карточек, у которых есть картинка и категория (каталог и превью).
  * **Конструктор:** `constructor(container: HTMLElement)`.
  * **Поля класса:**
    * `protected imageElement: HTMLImageElement` — элемент картинки товара.
    * `protected categoryElement: HTMLElement` — элемент текстовой категории.
  * **Методы (Сеттеры):**
    * `set image(value: string)` — устанавливает URL картинки.
    * `set category(value: string)` — устанавливает текст категории и динамически переключает CSS-модификатор фона (из объекта `categoryMap` файла `constants.ts`): `card__category_soft`, `card__category_hard`, `card__category_button`, `card__category_additional`, `card__category_other`.

* **Класс Карточка каталога (`CardCatalog`)** — наследуется от `CardWithImage`.
  * **Назначение:** Отображает карточку на главной витрине сайта внутри сетки товаров.
  * **Конструктор:** `constructor(container: HTMLElement, actions?: ICardActions)` — принимает контейнер и опциональный объект с обработчиками действий.
  * **События:** При клике на контейнер карточки генерирует событие `card:select` для открытия подробного превью.

* **Класс Карточка просмотра (`CardPreview`)** — наследуется от `CardWithImage`.
  * **Назначение:** Отображает подробную информацию о товаре в модальном окне.
  * **Конструктор:** `constructor(container: HTMLElement, actions?: ICardActions)`.
  * **Поля класса:**
    * `protected descriptionElement: HTMLElement` — элемент длинного текстового описания товара.
    * `protected buttonElement: HTMLButtonElement` — кнопка действия («Купить» / «Удалить из корзины» / «Недоступно»).
  * **Методы (Сеттеры):**
    * `set description(value: string)` — устанавливает подробное описание.
    * `set buttonText(value: string)` — устанавливает текст кнопки.
    * `set buttonDisabled(value: boolean)` — управляет доступностью кнопки.
  * **События:** Клик по кнопке генерирует событие `preview:buy-click`. Решение о тексте и доступности кнопки принимает Презентер на основе данных модели.

* **Класс Карточка в корзине (`CardBasket`)** — наследуется от `Card`.
  * **Назначение:** Отображает товар в виде компактной строки списка внутри модального окна корзины.
  * **Конструктор:** `constructor(container: HTMLElement, actions?: ICardActions)`.
  * **Поля класса:**
    * `protected indexElement: HTMLElement` — элемент порядкового номера товара в списке.
    * `protected buttonElement: HTMLButtonElement` — кнопка удаления.
  * **Методы (Сеттеры):**
    * `set index(value: number)` — устанавливает порядковый номер строки.
  * **События:** Клик по кнопке-мусорке генерирует событие `basket:remove-item` с payload `{ id: string }`.

#### 2. Группа компонентов Форм
Базовый класс берёт на себя рутинную работу с HTML-формами, а дочерние классы описывают конкретные поля ввода. Все DOM-элементы находятся в конструкторах и сохраняются в поля классов.

* **Базовый класс Форма (`Form<T>`)** — абстрактный.
  * **Назначение:** Выступает общим родителем для всех форм. Управляет валидацией, отображением ошибок и состоянием кнопки отправки.
  * **Конструктор:** `constructor(container: HTMLFormElement, events: IEvents)` — вешает общие слушатели событий `input` и `submit` один раз.
  * **Поля класса:**
    * `protected submitButton: HTMLButtonElement` — кнопка отправки формы.
    * `protected errorsContainer: HTMLElement` — контейнер для вывода текста ошибок валидации.
  * **Методы (Сеттеры):**
    * `set valid(value: boolean)` — включает или отключает доступность кнопки отправки (`disabled`).
    * `set errors(value: string[])` — принимает строго массив строк с ошибками валидации (`string[]`) и выводит их в контейнер, объединяя элементы массива через разделитель `". "`.
  * **События:** Генерирует динамические события изменения полей вида `[имя_формы].[имя_поля]:change` при вводе данных, и событие сабмита `[имя_формы]:submit`.

* **Класс Форма заказа (`FormOrder`)** — наследуется от `Form`.
  * **Назначение:** Компонент первой формы оформления заказа (Шаг 1: способ оплаты и адрес доставки).
  * **Конструктор:** `constructor(container: HTMLFormElement, events: IEvents)`.
  * **Поля класса:**
    * `protected cardButton: HTMLButtonElement` — кнопка выбора оплаты онлайн.
    * `protected cashButton: HTMLButtonElement` — кнопка выбора оплаты при получении.
    * `protected addressInput: HTMLInputElement` — поле ввода адреса доставки.
  * **Методы (Сеттеры):**
    * `set payment(value: TPayment | null)` — переключает CSS-модификатор активной кнопки `button_alt-active` для выбранного способа оплаты.
    * `set address(value: string)` — устанавливает значение в инпут адреса.
  * **События:** Клик по кнопкам оплаты генерирует `order.payment:change` с payload `{ field: 'payment', value: 'card' | 'cash' }`.

* **Класс Форма контактов (`FormContacts`)** — наследуется от `Form`.
  * **Назначение:** Компонент второй формы оформления заказа (Шаг 2: email и телефон).
  * **Конструктор:** `constructor(container: HTMLFormElement, events: IEvents)`.
  * **Поля класса:**
    * `protected emailInput: HTMLInputElement` — поле ввода email.
    * `protected phoneInput: HTMLInputElement` — поле ввода телефона.
  * **Методы (Сеттеры):**
    * `set email(value: string)` — устанавливает значение в инпут почты.
    * `set phone(value: string)` — устанавливает значение в инпут телефона.

#### 3. Универсальные компоненты страницы
* **Класс Шапка сайта (`Header`)**
  * **Назначение:** Отвечает за шапку сайта — счётчик товаров в корзине и кнопку открытия корзины.
  * **Конструктор:** `constructor(container: HTMLElement, events: IEvents)`.
  * **Поля класса:**
    * `protected counterElement: HTMLElement` — элемент цифрового счётчика на иконке корзины.
    * `protected basketButton: HTMLButtonElement` — кнопка-иконка корзины в шапке сайта.
  * **Методы (Сеттеры):**
    * `set counter(value: number)` — обновляет текстовое число на счётчике корзины.
  * **События:** Клик по кнопке корзины генерирует событие `basket:open`.

* **Класс Галерея (`Gallery`)**
  * **Назначение:** Управляет контейнером-сеткой для вывода карточек товаров.
  * **Конструктор:** `constructor(container: HTMLElement)`.
  * **Методы (Сеттеры):**
    * `set catalog(cards: HTMLElement[])` — принимает массив готовых отрендеренных карточек и вставляет их в галерею методом `replaceChildren`.

* **Класс Модальное окно (`Modal`)**
  * **Назначение:** Универсальный изолированный контейнер («коробка») для отображения любого контента (превью, корзины, форм) во всплывающем окне. Не имеет дочерних классов.
  * **Конструктор:** `constructor(container: HTMLElement)` — находит элементы и вешает слушатели закрытия один раз.
  * **Поля класса:**
    * `protected closeButton: HTMLButtonElement` — кнопка-крестик для закрытия окна.
    * `protected contentContainer: HTMLElement` — контейнер для динамического содержимого.
  * **Методы:**
    * `open(): void` — открывает окно добавлением модификатора `modal_active`.
    * `close(): void` — закрывает окно, убирая модификатор, и очищает контент.
    * `set content(value: HTMLElement)` — сеттер для динамической подмены вёрстки внутри модального окна.
  * **Закрытие:** по клику на крестик или по клику на сам оверлей (определяется через `evt.target === evt.currentTarget`). Блокировка скролла страницы при открытой модалке реализована через CSS-правило `.page:has(.modal_active) { overflow: hidden; }` в `page.scss`. Событий `modal:open` / `modal:close` в приложении нет.

* **Класс Корзина (`Basket`)**
  * **Назначение:** Отображает список выбранных товаров, их общую стоимость и кнопку перехода к оформлению.
  * **Конструктор:** `constructor(container: HTMLElement, events: IEvents)`.
  * **Поля класса:**
    * `protected listElement: HTMLElement` — элемент-контейнер для вывода списка строк товаров.
    * `protected totalElement: HTMLElement` — элемент для вывода суммарной стоимости.
    * `protected orderButton: HTMLButtonElement` — кнопка «Оформить».
  * **Методы (Сеттеры):**
    * `set items(cards: HTMLElement[])` — принимает массив строк товаров и заменяет содержимое списка. Если массив пуст, список пустеет, а CSS-правило `.basket__list:not(:has(> *))::before` автоматически показывает надпись «Корзина пуста».
    * `set total(value: number)` — устанавливает итоговую сумму в синапсах.
    * `set disabled(value: boolean)` — управляет доступностью кнопки «Оформить». Решение о блокировке принимает Презентер.
  * **События:** Клик по кнопке «Оформить» генерирует событие `order:open`.

* **Класс Окно успеха (`Success`)**
  * **Назначение:** Показывает финальное модальное окно об успешной оплате заказа бэкендом.
  * **Конструктор:** `constructor(container: HTMLElement, events: IEvents)`.
  * **Поля класса:**
    * `protected descriptionElement: HTMLElement` — элемент для вывода текста списанной суммы.
    * `protected closeButton: HTMLButtonElement` — кнопка «За покупками!».
  * **Методы (Сеттеры):**
    * `set total(value: number)` — устанавливает текст вида `Списано X синапсов`.
  * **События:** Клик по кнопке «За покупками!» генерирует событие `success:close`.

### Сводная таблица событий приложения
В приложении используется брокер событий (`EventEmitter`) для связи слоёв Представления (View) и Моделей данных (Model) через Презентер. Ниже описаны ключевые события, генерируемые в системе:

| Название события | Источник события | Описание события |
|------------------|------------------|------------------|
| `items:changed`  | `CatalogModel`   | Вызывается при успешной загрузке или обновлении товаров в модели каталога для перерисовки витрины |
| `preview:changed`| `CatalogModel`   | Сигнализирует о том, что выбран товар для подробного просмотра (без данных) |
| `basket:changed` | `BasketModel`    | Генерируется при любом изменении состава корзины (добавлении/удалении). Обновляет счётчик в шапке и итоговую сумму |
| `buyer:changed`  | `BuyerModel`     | Сигнализирует об изменении персональных или платежных данных покупателя |
| `card:select`    | `CardCatalog`    | Вызывается при клике на карточку товара на главной витрине для открытия её превью. Payload: объект товара `IProduct` |
| `preview:buy-click` | `CardPreview` | Вызывается при клике на кнопку «Купить» / «Удалить из корзины» в модальном окне превью товара |
| `basket:remove-item` | `CardBasket` | Вызывается при клике на кнопку-мусорку в списке корзины. Payload: `{ id: string }` |
| `basket:open`    | `Header`         | Вызывается при клике на иконку корзины в шапке сайта для открытия модального окна корзины |
| `order:open`     | `Basket`         | Вызывается при клике на кнопку «Оформить» для перехода к первому шагу заказа |
| `order.payment:change` | `FormOrder` | Вызывается при клике по кнопке выбора способа оплаты. Payload: `{ field: 'payment', value: 'card' \| 'cash' }` |
| `order.address:change` | `FormOrder` | Вызывается при вводе данных в поле адреса доставки. Payload: `{ field: 'address', value: string }` |
| `order:submit`   | `FormOrder`      | Вызывается при отправке формы первого шага (клик на «Далее») для перехода к контактам |
| `contacts.email:change` | `FormContacts` | Вызывается при вводе данных в поле email. Payload: `{ field: 'email', value: string }` |
| `contacts.phone:change` | `FormContacts` | Вызывается при вводе данных в поле телефона. Payload: `{ field: 'phone', value: string }` |
| `contacts:submit`| `FormContacts`   | Вызывается при отправке финальной формы контактов для отправки заказа на сервер через API |
| `success:close`  | `Success`        | Вызывается при клике на кнопку «За новыми покупками!» для закрытия окна успеха |

### Слой Презентера (Presenter)
Логика приложения, связывающая Модели данных (Model) и компоненты Представления (View), реализована в виде линейного кода управления внутри главного скрипта приложения `src/main.ts`. Файл выступает единой «диспетчерской вышкой», которая оркеструет работу всех независимых слоёв системы через брокер событий `EventEmitter` (паттерн Наблюдатель).

#### Архитектурный подход и роли в MVP:
* **Инверсия зависимостей:** Экземпляры моделей данных (`CatalogModel`, `BasketModel`, `BuyerModel`) и компонентов представления (`Header`, `Gallery`, `Modal`, `Basket`, `FormOrder`, `FormContacts`, `Success`, `CardCatalog`, `CardPreview`, `CardBasket`) инициализируются в корне файла `main.ts` и связываются друг с другом исключительно через интерфейсы и брокер событий. Компоненты ничего не знают о существовании друг друга.
* **Презентер не генерирует события.** Он подписывается на события от моделей и представлений и обрабатывает их: вызывает методы моделей для сохранения данных, а затем через метод `render({ ... })` передаёт свежие данные в компоненты представления.
* **Презентер не работает с разметкой.** Никаких обращений к DOM (`querySelector`, `classList`) в `main.ts` нет. Взаимодействие с модальным окном происходит исключительно через методы `modal.render()`, `modal.open()` и `modal.close()` — без прямого доступа к разметке.
* **Статичные компоненты создаются один раз.** `CardPreview` и `Success` инициализируются в начале скрипта. В обработчиках они только перерендериваются через `render`. Динамически создаются только `CardCatalog` и `CardBasket` — по одной на товар.
* **Рендер ≠ открытие.** В обработчиках открытия модалки (`basket:open`, `order:open`, `order:submit`) презентер только передаёт уже отрендеренную разметку в модалку и вызывает `modal.open()`. Сам рендер компонентов с данными из моделей выполняется в обработчиках событий изменения данных (`items:changed`, `basket:changed`, `buyer:changed`, `preview:changed`).
* **Синхронизация состояния View.** В Презентере определены вспомогательные функции `syncBasketView()`, `syncOrderForm()`, `syncContactsForm()`. Они получают актуальные данные из моделей и передают их в `render` соответствующего компонента. Функции вызываются:
  1. Один раз при инициализации приложения — для первичного рендера компонентов с данными пустых моделей.
  2. По событиям изменения данных в моделях:
     - `basket:changed` → `syncBasketView()`;
     - `buyer:changed` → `syncOrderForm()` и `syncContactsForm()` (обе формы синхронизируются всегда, чтобы их разметка соответствовала актуальному состоянию модели — в том числе после очистки данных покупателя при успешном заказе).

#### Основные сценарии логики, обрабатываемые в `main.ts`:
1. **Инициализация каталога:** При старте приложения Презентер вызывает метод `api.getProductList()`. Полученный массив товаров записывается в `CatalogModel`, которая генерирует событие `items:changed`. Презентер ловит его, создаёт экземпляры карточек `CardCatalog` для каждого товара (с полным URL картинки, сформированным с учётом `CDN_URL`) и отрисовывает их в галерее через `gallery.render({ catalog })`.

2. **Открытие превью товара:** При клике на карточку витрины генерируется `card:select` с объектом товара. Презентер вызывает `catalogModel.setPreview(item)`, модель эмитит `preview:changed`. В обработчике Презентер получает товар через `catalogModel.getPreview()`, вычисляет текст и доступность кнопки на основе `item.price` и `basketModel.isInBasket()`, передаёт данные в `cardPreview.render({ ... })` и вызывает `modal.open()`.

3. **Управление корзиной:** Клик по кнопке покупки в превью генерирует `preview:buy-click`, клик по мусорке в корзине — `basket:remove-item`. Презентер вызывает соответствующие мутирующие методы `BasketModel`. Модель генерирует `basket:changed` — Презентер обновляет счётчик через `header.render({ counter })` и перерисовывает содержимое корзины через `syncBasketView()`. Ререндер происходит независимо от того, открыта ли корзина в данный момент.
   * При клике «Оформить» (событие `order:open`) Презентер передаёт уже отрендеренную форму в модалку (`modal.render({ content: orderForm.render() })`) и открывает её. Сам рендер формы с данными из модели выполняется в обработчике `buyer:changed`.
   * При открытии корзины (`basket:open`) Презентер передаёт уже отрендеренную разметку корзины в модалку и открывает её. Ререндер содержимого корзины — только по `basket:changed`.

4. **Валидация и оформление заказа:**
   * При вводе данных (`input`) формы эмитят события `order.address:change`, `contacts.email:change` и т.п. Презентер через общий обработчик `onBuyerFieldChange` вызывает `buyerModel.setBuyerField()`.
   * По событию `buyer:changed` Презентер вызывает `syncOrderForm()` и `syncContactsForm()` — обе формы синхронизируются с актуальным состоянием модели. Функции берут данные из `buyerModel.getBuyerData()`, получают ошибки через `buyerModel.validateBuyerData()` и передают всё в `render({ payment, address, valid, errors })` соответствующей формы. Это гарантирует, что при следующем открытии формы пользователь увидит реальные данные (в том числе пустые после успешного оформления заказа).
   * При сабмите первой формы (событие `order:submit`) Презентер передаёт уже отрендеренную форму контактов в модалку и открывает её.

5. **Отправка заказа на сервер:** При сабмите формы контактов Презентер собирает единый объект заказа `IOrder` (сливает данные покупателя, массив ID товаров и итоговую сумму). Отправляет данные на сервер методом `api.createOrder()`. При успешном ответе бэкенда Презентер очищает модели корзины и покупателя и выводит окно успешной оплаты `Success` с суммой, полученной от сервера. При ошибке — выводит сообщение в форме контактов.

6. **Закрытие окна успеха:** Клик по кнопке эмитит `success:close`. Презентер вызывает `modal.close()`.